---
title: Cider
type: company-profile
updated: 2026-05-04
---

# Cider

## 概览
- 方向：后端开发（已 OC）
- 难度：聊天 + 八股 + 一道算法题；建议"专注业务"
- 一句话画像：项目讲低代码 / SQL 优化 / RPC 自定义 TCP；八股是 MySQL 隔离级别 + MVCC

## 面经文件
- [[小鹅通往年面经+我的面经/小鹅通二面面经（结果未知）Cider一面面经（通过）]] —— 低代码平台 + 低代码 + AI 看法 + SQL 优化（select / 索引 / Join / 插入）+ 索引怎么加（避免失效 + 联合索引顺序 + 加了再删）+ 性别字段不该加索引（区分度低）+ 验证二叉搜索树 + 改成迭代 + RPC 为什么基于 TCP 自定义（速度 / 传输效率）+ TCP 滑动窗口理解 + 大众点评追问；面试官建议专注业务项目
- [[Cider面经/1]] —— MySQL 4 种事务隔离级别详解 + 默认 RR + 各自机制 + 脏读/不可重复读/幻读三种异常的具体例子 + InnoDB vs MyISAM 对比 + MVCC 原理（隐藏字段 trx_id/roll_pointer + Undo Log + ReadView 三可见性规则）
- [[Cider面经/2]] —— stub（个人复盘"还要学 MySQL 和 JUC"）

## 考察重点（按出现次数排序）
1. **MySQL 事务隔离级别 + MVCC** —— 4 级隔离 + 各自机制（RU 不用 MVCC / RC 每次 SELECT 新 ReadView / RR 事务开始一个 ReadView 复用 / Serializable 串行加锁）+ 三种异常具体例子
2. **InnoDB vs MyISAM** —— 事务 / 行锁表锁 / 外键 / 崩溃恢复 / 适用场景对比
3. **SQL 优化策略** —— select 字段精简、索引、Join 改写、插入优化
4. **索引设计原则** —— 避免失效（最左前缀 / 函数 / 范围后失效）+ 联合索引顺序 + 区分度低字段（性别）不加
5. **RPC 自定义 TCP 协议的动机** —— 速度 / 传输效率
6. **TCP 滑动窗口** —— 流量控制
7. **算法** —— 验证二叉搜索树（递归 + 改迭代用栈）

## 真题摘录（值得背的）

> **可重复读 vs 读已提交在 ReadView 上的差异？** 出处 [[Cider面经/1]]
> RC：每次 SELECT 都生成新的 ReadView，所以同一事务内不同时刻能看到其他事务新提交的数据（不可重复读）。RR：事务内第一次 SELECT 生成 ReadView 并复用，所以多次读结果一致。InnoDB 在 RR 下还通过 next-key lock（间隙锁）防止当前读时的幻读。

> **为什么不该在性别字段加索引？** 出处 [[小鹅通往年面经+我的面经/小鹅通二面面经（结果未知）Cider一面面经（通过）]]
> 区分度太低（男女各占一半），索引筛掉的行少。索引价值在于"用少量索引读 + 少量回表"换"避免全表扫描"，区分度低时优化器宁可走全表扫描。

## 复习清单（双链）
- [[MySQL索引与执行计划]]
- [[MySQL-MVCC与隔离级别]]
- [[InnoDB事务与锁]]
- [[Java并发集合]]
- [[Java锁体系]]
- [[TCP与HTTPS]]
- [[算法题索引]]
- [[Java-RPC框架]]
- [[黑马点评秒杀]]
