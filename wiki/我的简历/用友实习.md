---
title: 用友实习
type: resume-section
period: 2024/07 – 2024/09
updated: 2026-05-04
---

# 用友实习（2024/07 – 09）

## 简历原文

> 参与人力资源系统后端模块开发，服务 **8K+ 员工**，负责跨系统数据迁移与同步能力建设，保障数据的一致性与完整性。

> 基于 **gRPC 和 Python 脚本**实现迁移服务，通过**断点续传与增量标记机制**，支持全量/增量迁移，**批处理写入 PostgreSQL**。

> 利用 **Redis 缓存高频字段，缓存命中率约 90%**，在峰值流量下为 PostgreSQL 减少约 **1K QPS** 负载，**p95 延迟下降约 40%**。

> 通过**慢查询、pg_stat_statements 和 EXPLAIN ANALYZE** 定位 PostgreSQL 性能瓶颈，针对热点查询重构索引并改写 SQL，消除 **N+1 查询**，提高 SQL 效率。

## STAR 自陈

- **S**：用友 8K+ 员工人力系统从旧库迁到新库，需要不停机 + 一致性 + 可回滚。秋招高峰期峰值流量大，PostgreSQL 成瓶颈。
- **T**：搭迁移服务 + 优化 PG 查询性能 + Redis 缓存层。
- **A**：1) gRPC + Python 脚本搬数据，分批事务 + checkpoint 实现断点续传；2) updated_at 时间戳做增量标记，upsert 幂等；3) 迁移完成后 count + hash 校验完整性；4) Redis 缓存组织架构 / 岗位字典 / 流程配置 / 用户权限 4 类高频读字段；5) Cache Aside + TTL 兜底；6) 用慢查询 + pg_stat_statements + EXPLAIN ANALYZE 定位 PG 瓶颈，重构索引、消除 N+1。
- **R**：缓存命中率 90%，PG QPS 下降 1K+，p95 延迟下降 40%。

## 关键数据 / 名词
- **规模**：8K+ 员工
- **栈**：gRPC + Python + PostgreSQL + Redis
- **缓存**：命中率 90%，QPS -1K，p95 -40%
- **迁移机制**：断点续传 + 增量标记（updated_at）+ 批处理 + 全量校验
- **PG 优化**：慢查询日志 + pg_stat_statements + EXPLAIN ANALYZE，N+1 消除

## 跨简历联动
- 详细 Q&A 库：[[用友数据迁移]]
- 主题文章：[[数据迁移与一致性]] · [[PostgreSQL要点]] · [[缓存一致性]]
- 被深挖公司：[[公司/快手]] [[公司/阿里云]] [[公司/蚂蚁]] [[公司/虾皮-Shopee]]
