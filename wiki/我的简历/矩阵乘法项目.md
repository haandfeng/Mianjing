---
title: 高性能矩阵乘法项目
type: resume-section
period: 个人项目
updated: 2026-05-04
note: wiki 中此前未覆盖的项目，新加
---

# 高性能矩阵乘法（GitHub）

## 简历原文

> 在 **ARM 平台**实现优化版 **DGEMM** 内核，结合**多级 cache blocking、数据打包与基于 SVE 的微内核设计**，**峰值性能达 22 GFLOPS**，较朴素三重循环实现**提速超过 20 倍**，在大规模矩阵上**略优于 OpenBLAS**。

> 开发基于 **CUDA 的 tiled GEMM** 内核，采用**共享内存 tiling、二维指令级并行 (2-D ILP) 和如 32×8 的线程块形状调优**，在 **NVIDIA T4 GPU** 上实现约 **3.6–3.8 TFLOPS**，相对朴素实现**提速约 15 倍**，性能接近 **cuBLAS 的约 80%**。

## STAR 自陈

- **S**：DGEMM (Double GEMM) 是 BLAS Level-3 核心运算，朴素三重循环受 cache miss 和 SIMD 利用率限制，性能远低于硬件理论峰值。
- **T**：在 ARM (SVE 指令集) 和 NVIDIA T4 上分别实现接近 vendor BLAS 的高性能 GEMM 内核。
- **A（ARM）**：(1) 多级 cache blocking（L1/L2/L3 分块）；(2) 数据打包（A/B 矩阵重排成连续访存）；(3) 基于 SVE（Scalable Vector Extension）的微内核手写汇编。
- **A（CUDA）**：(1) 共享内存 tiling 减少 global memory 访问；(2) 2-D ILP（每个线程算多个输出元素）；(3) 调优线程块形状到 32×8。
- **R**：ARM 22 GFLOPS（朴素 ×20+，略优于 OpenBLAS）；CUDA 3.6–3.8 TFLOPS（朴素 ×15，~80% cuBLAS）。

## 关键数据 / 名词
- **ARM 栈**：SVE 指令集 + cache blocking + 数据打包
- **CUDA 栈**：tiled GEMM + shared memory + 2-D ILP + 32×8 线程块
- **数据**：ARM 22 GFLOPS（vs 朴素 ×20，略优于 OpenBLAS）
- **数据**：CUDA T4 3.6–3.8 TFLOPS（vs 朴素 ×15，~80% cuBLAS）

## 跨简历联动
- 与 [[Arm实习]] 共享技术栈（SIMD、Linux 性能调优思维）。CSAPP / 链接器 / 体系结构功底的体现。
- **wiki/项目/ 中没有专门 Q&A 页面**——因为面经里很少被深挖（更多被深挖的是 ARM Profiling 和 RAG），但简历上有这条说明你具备 HPC 背景。
- 面试时可以作为"我对底层硬件有深入理解"的支撑材料。

## 可能被问到的方向（预测）
- 为什么 cache blocking 能加速？（局部性原理 + L1/L2/L3 hierarchy）
- SVE 和 NEON 区别？
- CUDA shared memory 和 global memory 性能差异？
- 为什么 32×8 而不是 16×16？（ILP / 寄存器压力 / occupancy 三者权衡）
- 为什么没到 cuBLAS 100%？（手写 vs vendor 调优、tensor core 利用、PTX 级优化）
